name: Daily Tips (test)

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: self-hosted

    steps:
      - name: Say hello
        shell: powershell
        run: |
          Write-Host "Runner toimii, tämä on testiajo."
