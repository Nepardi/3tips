name: Daily Tips (test)

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: self-hosted

    steps:
      - name: Say hello
        shell: pwsh
        run: |
          Write-Host "Runner toimii, tämä on testiajo."

