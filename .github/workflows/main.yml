name: Daily Tips (test)

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: self-hosted

    steps:
      - name: Hello
        shell: powershell
        run: |
          Write-Host "Testi toimii."
